import { OrderHostDirective } from './order-host.directive';

describe('OrderHostDirective', () => {
  it('should create an instance', () => {
    const directive = new OrderHostDirective();
    expect(directive).toBeTruthy();
  });
});
